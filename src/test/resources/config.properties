# Application

bootstrap.servers=localhost:9092

application.id=myapp

# Topics

source.topic=topic_source
dest.topic=topic_enriched
lookup.table.topic=topic_lookup
dlq.topic=topic_dlq
retry.topic=topic_retry
min.insync.replicas=2

# classes
# lookup.table.class=com.nttdata.poc.model.Domain
# source.class=com.nttdata.poc.model.Activity
# output.class=com.nttdata.poc.model.ActivityEnriched

# State Store TTL

state.store.dir=/tmp/state-11
state.store.ttl.check.enabled=true
state.store.ttl.check.period.ms=14500
state.store.ttl.ms=12000


# In-memory cache
cache.max.bytes.buffering=1111

# This configures the commit.interval.ms of the topology
commit.interval.ms=3333


# Basic configuration at the moment
# REST Endpoint

endpoint.url=https://fake.url
